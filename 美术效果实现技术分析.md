# The Plan — 美术效果的关键实现技术

> 引擎：Unity 4.5.5p5 / Direct3D 9 / Gamma 色彩空间 / 1920×1080
> 取证方式：只读解析 SerializedFile，并从 43 个 Shader 对象中提取**完整 ShaderLab 源码**逐条核对渲染状态
> 本篇聚焦「技术手法」，资产组织见《ThePlan_美术资产组织分析.md》

## 一、一句话结论

**这是一套"用美术资产替代引擎计算"的 2.5D 伪立体方案**：不做烘焙光照、不做真实景深、不做实时体积光，把预算全部压在贴图、绘制顺序和后期调性上。全场景只有 **21 个自制网格、1 个光源、0 张光照贴图**，却靠 13 级手工渲染队列 + 顶点摆动着色器 + 预烘焙模糊贴图撑出了完整的林间空间感。

## 二、核心手法一：renderQueue 手工深度分层

场景里 345 个 MeshRenderer 的 **lightmapIndex 全部为 255（即无光照贴图）**，且几乎所有透明材质都 `ZWrite Off`。这意味着**深度缓冲不参与遮挡判断**——前后关系完全由 `renderQueue` 决定。

作者把同一套"透明无光照"着色器复制成了 7 份，仅改队列值，形成从远到近的层级：

| 队列 | 层内容 | 着色器 | 混合 |
|---|---|---|---|
| AlphaTest (2450) | 硬边镂空叶片、蘑菇 | Transparent/Cutout/Diffuse | 不透明裁剪 |
| Transparent-100 | 背景幕（平铺 / 顶部 / 首尾） | Transparent Unlit -100 | 常规 alpha |
| Transparent-50 | 远景树冠、灌木、树干 | Transparent Unlit -50 | 常规 alpha |
| Transparent-25 | 中景树枝、黑色剪影 | Transparent Unlit -25 | 常规 alpha |
| Transparent-22 | **远景散景光斑** | Shimmering Bokeh Far | **加色** |
| Transparent-20 | 远层树叶 | Transparent Unlit -20 | 常规 alpha |
| Transparent-16 | **动态光柱** | Light Shaft (Animated) | **加色** |
| Transparent-15 | **近景散景光斑** | Shimmering Bokeh Near | **加色** |
| Transparent-10 | 结局星云、引导光柱 | Unlit Diffuse Transparent-10 | 常规 alpha |
| Transparent (3000) | 草、蛛网、主角苍蝇 | TransparentWaving / SpiderWeb | 常规 alpha |
| Transparent+2 | 叠加叶片、苍蝇投影 | Multiply | 常规 alpha |
| Transparent+10 | 前景树枝 | Transparent Unlit +10 | 常规 alpha |
| Transparent+100 | 最前实心前景 | Transparent Unlit +100 | 常规 alpha |

这套做法的代价是**无法动态穿插**（一个物体不能同时"在树叶前又在树枝后"），但换来的是零深度冲突、零排序 bug、绘制顺序完全可控——对固定机位的单场景体验是最优解。

## 三、核心手法二：用贴图预烘焙代替实时计算

包里有 `DepthOfField34`、`DepthOfFieldScatter`、`GlobalFog`、`SSAOEffect`、`AmbientObscurance`、`Tonemapping`、`ColorCorrectionCurves` 等 **40+ 个 Unity ImageEffects 脚本**，但场景里实际挂载的后期只有 4 个。也就是说作者试过真实后期方案，最后全部退回美术侧解决：

| 想要的效果 | 实际实现 | 证据 |
|---|---|---|
| 景深虚化 | **预烘焙模糊贴图**：同一物体准备清晰版与模糊版两张贴图 | `TrunkSmallBlur`、`Leaf Particle Blurred`、`SpiderWeb_Blurred`、`bush_01v1_faded` |
| 体积光柱 | **UV 滚动贴图 + 加色混合**，不用任何光线步进 | `Light Shaft (Animated)`：`_ScrollTex`、`_TimeScale`、`_Tile` 属性，`Blend SrcAlpha One` |
| 大气雾 | **完全关闭**——`RenderSettings.m_Fog = False`，纵深靠背景幕贴图分层 | 雾参数（15.9→59.5）留着但没启用 |
| 环境光遮蔽 | **无**，靠贴图自身明暗画死 | `m_Lightmaps` 长度 0，无 light probe |
| 灯泡玻璃感 | 1.3 万顶点高模 + `Cull Front` 只画背面，制造双层玻璃厚度 | `LightBulb_Optimized` |

## 四、光照：极简到近乎放弃

- **光源只有 1 个**：Point Light，青蓝色 (0.70, 0.99, 1.0)，强度 3.0，范围 20 —— 就是那盏灯泡。
- **0 张光照贴图、0 个 light probe**，全场景 345 个渲染器 `lightmapIndex = 255`。
- 环境光固定为 0.2 灰（`m_AmbientLight`）。
- 主体材质走**自发光/无光照**路线：`Self-Illumin/Pure Color`、`Unlit/Texture`、`Unlit/Transparent`、`Transparent Unlit` 系列占了绝大多数。
- `castShadows = True` 的有 343 个，但**唯一光源是点光源**，实际阴影贡献极小——阴影开关更像是残留的默认设置。

结论：画面明暗**几乎全部来自贴图画死的明暗关系**，引擎光照只负责灯泡附近的一小圈。

## 五、自定义着色器（43 个中真正关键的 4 个）

UnityPy 直接读出的 Shader 名有 4 个为空，从 `m_Script` 源码第一行的 `Shader "..."` 还原后，恰好是这套美术的四个技术支点：

| 着色器 | 作用 | 关键属性 |
|---|---|---|
| **TransparentWaving** | GPU 顶点摆动，风吹草叶 | `_WaveSpeedX/Y`、`_WaveStrength`、`_UVXOffset/_UVYOffset`，队列 Transparent。供 `grass_01v1/v2/v2_2`、`Leaves_01v1/01v2/03v1/04v1` 使用 |
| **Light Shaft (Animated)** | 光柱流动 | `_ScrollTex` 滚动贴图、`_TimeScale`、`_Tile`、`_Multiplier`，`Blend SrcAlpha One`，队列 Transparent-16 |
| **SpiderWeb** | 蛛网形变与破坏分级 | `_WebTexLevel`（X=无破损 / Y=部分 / Z=大量）、`_HighlightStr`、`_VariationTile`，配合蒙皮网格使用 |
| **Multiply** | 正片叠底叠加层 | 队列 Transparent+2，用于叠加叶片与 `Fly_Shadow` 苍蝇投影 |

另外 `Particles-ShimmeringBokeh_Near / Far` 是两个手写的散景光斑着色器（队列 -15 / -22），专门做前景虚焦光点——这又是一处"用粒子贴图冒充光学散景"。

## 六、镜头语言

7 个 Camera，**视场角只有 15° / 20° / 30°**（常规游戏是 60~90°）。15° 是长焦镜头，会带来强烈的空间压缩感与"微距摄影"观感——这是把一只苍蝇拍出"巨物感"的关键。

- 只有 **1 个相机开了 HDR**（`m_HDR = True`），正是承接 Bloom 的那一台。
- near = 0.3 / far = 1000，清屏用 Skybox 但 `m_SkyboxMaterial` 为空。
- 无天空盒、无反射探针；环境反射靠 **1 张 1.5 MB 的 Cubemap** + `CubeMapGenerator` 脚本。

## 七、实际生效的后期处理链

| 挂载脚本 | 配套着色器 | 效果 |
|---|---|---|
| `BloomAndLensFlares` | `BrightPassFilter` → `SeparableBlur` / `SeparableBlurPlus` / `BlurEffectConeTaps` → `LensFlareCreate` + `MultiPassHollywoodFlares` → `Blend` / `BlendOneOne` | 高光提取、多级模糊、好莱坞式多通道耀斑、最终合成 |
| `Vignetting` | `VignettingShader` / `VignetteShader` + 3 MB 的 `Vignette` 遮罩贴图 | 暗角 |
| `NoiseAndGrain` | `NoiseAndGrain` / `NoiseAndGrainDX11` | 胶片颗粒 |
| `BlurEffect` | `BlurEffectConeTaps` | 菜单/转场模糊（配合 `AnimationClip` 做淡入淡出） |
| `VignetteOverlay` | `Hidden/ScaryFX Distortion and Overlay` | 结局的扭曲叠加 |

另有 `ChromaticAberrationShader`（色差）着色器已打包，但未见相机挂载，可能在特定事件由脚本动态启用。

**这套后期的定位很明确**：不追求真实，追求"胶片感 + 老旧镜头"的调性——暗角、颗粒、过曝光晕、耀斑，把低多边形与低分辨率贴图的粗糙感包装成风格。

## 八、角色与动态表现

| 对象 | 技术 | 组件证据 |
|---|---|---|
| 主角苍蝇 | 骨骼蒙皮动画（3647 顶点）+ 飞行拖尾 | 3 个 `SkinnedMeshRenderer`（Fly_Body / Fly_Wing）、1 个 `TrailRenderer`（材质 `Default-Particle`） |
| 苍蝇翅膀 | 独立半透明材质，双面渲染 | `Fly_Wing_TransparentUnlit`：`Cull Off`、`Blend SrcAlpha OneMinusSrcAlpha` |
| 蛛网 | **蒙皮网格 + 骨骼驱动弹动**，而非普通网格 | `SkinnedMeshRenderer` 使用材质 `SpiderWeb1` |
| 场景摆动 | 物理关节 | 34 `Rigidbody`、18 `CharacterJoint`、7 `FixedJoint`，脚本 `Wind` ×11、`ActivateWind` ×11、`ActivateLeafs` ×5、`LeafScript` ×7 |
| 粒子 | 27 个 `ParticleSystem`，走加色/叠底粒子着色器 | 材质 `Wind Particle` 被引用 12 次、`Ending Nebula`、`Star`、`Navi` 等 |
| 蜘蛛网破坏 | 脚本驱动的破坏分级 | 脚本 `SpiderWeb`、`TimedDestroy`、`ExplosionForce`、`NegativeForce`（×59） |

## 九、性能侧的工程手段

- **静态批处理**：构建期生成 `Combined Mesh (root: scene)`（9317 顶点 / 15400 面），把大量静态装饰物合并。
- **实例化复用**：345 个 MeshFilter 只引用 21 个网格，植被全靠重复摆放。
- **遮挡剔除**：场景含 `OcclusionCullingSettings`，相机 `m_OcclusionCulling = True`。
- **多线程渲染**：`m_MTRendering = True`，D3D9 路径（`m_UseDX11 = False`）。
- **音频占 33 MB**，与贴图同量级；130 个 `AudioSource` + 22 个 `AudioReverbFilter` + 4 个 `AudioReverbZone`，氛围靠声音补足画面信息量的不足。

## 十、这套路线的可借鉴之处与边界

**可借鉴**：固定机位、单场景、弱交互的体验型作品，用「人工分层 + 贴图烘焙 + 后期调性」能在极低几何预算下做出高完成度画面。核心是把**不可控的引擎计算换成可控的美术产出**。

**边界**：
1. 深度靠绘制顺序，一旦加入自由移动的相机或需要动态穿插的物体，层级立刻穿帮。
2. 所有纵深信息烘焙进贴图，换视角、换时段、加动态天气都要重做素材。
3. 光照完全画死，物体无法真正响应环境变化（如飞进阴影变暗）。
4. 未压缩贴图 21.4 MB、重复资产 6.5 MB，是这套"贴图堆叠"路线必然附带的体积代价。
